import { CustomModal } from 'components/theme/CustomModal/CustomModal';
import styled from 'styled-components';

export const SPaymentErrorModal = styled(CustomModal)`
  button:is(.default, .primary) {
    white-space: normal;
  }
`;
