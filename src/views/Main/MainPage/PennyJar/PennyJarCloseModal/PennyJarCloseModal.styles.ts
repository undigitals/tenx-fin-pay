import { CustomModal } from 'components/theme/CustomModal/CustomModal';
import styled from 'styled-components';

export const SPennyJarCloseModal = styled(CustomModal)`
  button.default {
    margin-top: 35px;
    font-size: 16px;
  }
`;
